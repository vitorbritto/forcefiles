#!/bin/bash

# ------------------------------------------------------------------------------
# | Install aplications                                                        |
# ------------------------------------------------------------------------------

run_apps() {

    # Install native apps
    brew tap phinze/homebrew-cask
    brew install brew-cask

    local -a list_apps=(
        "appcleaner"
        "firefox"
        "sublime-text"
        "filezilla"
        "imageoptim"
        "dropbox"
        "mou"
        "sparrow"
        "feeds"
        "screenflow"
        "limechat"
        "arduino"
        "google-drive"
    )

    brew cask install $list_apps
    [[ $? ]] && e_success "All apps installed successfully!"

}
